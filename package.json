{
  "name": "action-revel-messages",
  "version": "1.0.0",
  "description": "Check revel messages for issues",
  "main": "index.js",
  "scripts": {
    "lint": "eslint .",
    "prepare": "ncc build index.js -o dist --source-map --license licenses.txt",
    "test": "jest",
    "all": "npm run lint && npm run prepare && npm run test"
  },
  "author": "Brett Mayson <brett@vivvo.com>",
  "license": "MIT",
  "dependencies": {
    "@actions/core": "^1.6.0",
    "walkdir": "^0.4.1"
  },
  "devDependencies": {
    "@vercel/ncc": "^0.31.1",
    "eslint": "^8.1.0",
    "jest": "^26.1.0"
  }
}
